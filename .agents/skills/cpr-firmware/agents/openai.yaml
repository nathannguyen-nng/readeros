interface:
  display_name: "CPR Firmware"
  short_description: "Firmware-safe readerOS changes"
  default_prompt: "Use $cpr-firmware to make a safe firmware change in readerOS."
policy:
  allow_implicit_invocation: true
